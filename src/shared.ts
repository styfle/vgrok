export const LOCAL_PORT = 8000; // Port for the local server
export const REMOTE_PORT = 3000; // Port for the remote tunnel server
export const TUNNEL_URL = `ws://localhost:${REMOTE_PORT}/ws`; // URL of the remote tunnel server